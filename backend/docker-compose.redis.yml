version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: vela-redis
    ports:
      - "6389:6389"
    command: redis-server --appendonly yes --port 6389
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
