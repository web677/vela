# Vela 本地开发 - 快速启动指南

## 🚀 最简单的启动方式

### Windows 用户

双击根目录下的 `start.bat` 文件，脚本会自动：

1. 检查 Node.js 安装
2. 检查并创建环境变量文件
3. 安装依赖（如果需要）
4. 在新窗口启动后端
5. 在新窗口启动前端

**或者手动执行**：

```bash
# 在项目根目录
.\start.bat
```

### 首次运行准备

#### 1. 配置后端环境变量

编辑 `backend\.env` 文件：

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
```

> 💡 从 [supabase.com](https://supabase.com) 项目的 Settings > API 获取密钥

#### 2. 确认前端环境变量

检查 `frontend\.env.local` 文件：

```env
VITE_API_URL=http://localhost:3000/api
```

---

## 📋 手动启动步骤

### 方式一：两个终端窗口

**终端 1 - 后端**：

```bash
cd D:\workdata\vela\backend
npm install          # 首次运行
npm run start:dev    # 启动开发服务器
```

**终端 2 - 前端**：

```bash
cd D:\workdata\vela\frontend
npm install          # 首次运行
npm run dev         # 启动开发服务器
```

### 方式二：使用 VSCode

1. 打开项目根目录 `D:\workdata\vela`
2. 打开集成终端
3. 分割终端（点击终端右上角的分割图标）
4. 在左侧终端运行后端
5. 在右侧终端运行前端

---

## ✅ 验证启动成功

### 1. 后端验证

看到以下输出表示成功：

```
========================================
🚀 Vela Backend API
========================================
📍 URL: http://localhost:3000/api
🌍 Environment: development
🔐 CORS Origin: http://localhost:5173
✅ Server started successfully!
========================================
```

**测试接口**：

- 浏览器访问: http://localhost:3000/api/categories
- 应该返回 JSON 数据（可能是空数组 `[]`）

### 2. 前端验证

看到以下输出表示成功：

```
VITE v4.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**测试页面**：

- 浏览器访问: http://localhost:5173
- 应该看到 Vela 首页，带紫色渐变标题

---

## 🔧 常见问题

### 问题 1: 端口被占用

**错误**：`Port 3000 is already in use`

**解决方案**：

```bash
# 查找占用端口的进程
netstat -ano | findstr :3000

# 结束进程（替换 PID）
taskkill /PID <进程ID> /F

# 或者修改端口
# 编辑 backend\.env 文件，修改 PORT=3001
```

### 问题 2: 依赖安装失败

**解决方案**：

```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules
del /s /q node_modules
rmdir /s /q node_modules

# 重新安装
npm install
```

### 问题 3: 无法连接后端

**检查清单**：

- [ ] 后端服务是否正在运行
- [ ] 后端端口是否为 3000
- [ ] 前端 `.env.local` 中的 API URL 是否正确
- [ ] 浏览器控制台是否有 CORS 错误

### 问题 4: Supabase 连接失败

**检查清单**：

- [ ] `.env` 中的 `SUPABASE_URL` 是否正确
- [ ] `SUPABASE_ANON_KEY` 和 `SUPABASE_SERVICE_ROLE_KEY` 是否正确
- [ ] Supabase 项目是否在运行状态
- [ ] 网络连接是否正常

---

## 🛑 停止服务

### 使用 start.bat 启动的

直接关闭对应的命令窗口，或在窗口中按 `Ctrl + C`

### 手动启动的

在各自的终端中按 `Ctrl + C`

---

## 🔄 重启服务

### 完全重启

1. 停止所有服务（Ctrl + C）
2. 再次运行启动命令

### 仅重启后端

```bash
# 在后端终端按 Ctrl + C 停止
# 然后重新运行
npm run start:dev
```

### 仅重启前端

```bash
# 在前端终端按 Ctrl + C 停止
# 然后重新运行
npm run dev
```

---

## 📝 开发模式 vs Docker 模式

| 特性     | 开发模式 | Docker 模式 |
| -------- | -------- | ----------- |
| 热重载   | ✅ 支持  | ❌ 不支持   |
| 启动速度 | 快       | 较慢        |
| 环境隔离 | 无       | ✅ 完全隔离 |
| 调试     | 容易     | 较复杂      |
| 推荐场景 | 日常开发 | 测试部署    |

**推荐**：日常开发使用开发模式，部署前使用 Docker 模式测试。

---

## 🎯 下一步

启动成功后，您可以：

1. **注册账号**: 访问 http://localhost:5173/register
2. **浏览产品**: 访问 http://localhost:5173/products
3. **测试 API**: 使用 Postman 或浏览器测试接口

---

## 💡 开发技巧

### VSCode 推荐扩展

- **ESLint** - 代码检查
- **Prettier** - 代码格式化
- **Volar** - Vue 3 支持
- **REST Client** - API 测试

### 快捷命令

创建 `package.json` 在根目录：

```json
{
  "scripts": {
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
```

然后可以使用：

```bash
npm run dev:backend
npm run dev:frontend
```

---

**快速启动脚本已创建**: `D:\workdata\vela\start.bat`
**直接双击即可启动整个项目！** 🚀
